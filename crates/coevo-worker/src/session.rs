pub struct WorkerSessionService;
impl WorkerSessionService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for WorkerSessionService {
    fn default() -> Self {
        Self::new()
    }
}
